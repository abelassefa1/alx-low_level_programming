#include <stdio.h>
/**
 * main - main block
 * Description:  prints the alphabet in lowercase, and then in uppercasde
 * Return: Always (Success)
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		putchar (c);
		putchar('\n');
		return (0);
}
